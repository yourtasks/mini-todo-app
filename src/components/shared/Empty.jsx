const Empty = ({ title }) => {
  return (
    <div className="h-full w-full flex items-center justify-center font-semibold">
      <div></div>
      <h1>{title}</h1>
    </div>
  );
};

export default Empty;
